#Mouse config ---------------------
# ドライバは cormoran/zmk-driver-pmw3610-with-custom-studio-rpc。
# badjeff/zmk-pmw3610-driver から差し替えたので、CONFIG の接頭辞は
# CONFIG_PMW3610_* であって CONFIG_PMW3610_ALT_* ではない。
# 軸の反転・入れ替えは devicetree ではなくここ（または Studio の詳細設定）で行う。
# CONFIG_PMW3610_INVERT_X=y
# CONFIG_PMW3610_INVERT_Y=y
# CONFIG_PMW3610_SWAP_XY=y
# CONFIG_PMW3610_REPORT_INTERVAL_MIN=10
# CONFIG_PMW3610_LOG_LEVEL_DBG=y
# CONFIG_PMW3610_INIT_POWER_UP_EXTRA_DELAY_MS=300 // <--see Troubleshooting

CONFIG_ZMK_RUNTIME_INPUT_PROCESSOR=y
CONFIG_PMW3610_SMART_ALGORITHM=y

# PMW3610 のパラメータ（cpi / レストモードの各時間 / force_awake /
# smart_algorithm など）を、既存の zmk-feature-custom-settings の汎用 RPC 経由で
# DYA Studio の「詳細設定」タブから実行時に変更・保存できるようにする。
# サブシステム ID は cormoran__pmw3610、キーは cpi@trackball のような形。
# 項目一覧は docs/dya-studio.md を参照。
CONFIG_ZMK_PMW3610_CUSTOM_SETTINGS=y

# 【この2行を消すと詳細設定にPMW3610が出なくなる】
# CUSTOM_SETTINGS だけでは駄目で、STUDIO_RPC も要る。Kconfig 上は
# ZMK_PMW3610_CUSTOM_SETTINGS が depends on ZMK_CUSTOM_SETTINGS だけなので
# 一見不要に見えるが、実際には zmk-feature-custom-settings 側の
# src/studio/custom_settings_handler.c にある custom_subsystem_index_for_identifier()
# が、設定を protobuf に詰める前に「その custom_subsystem_id が RPC サブシステムと
# して登録されているか」を走査し、無ければ -ENOENT を返してその設定を黙って捨てる。
# STUDIO_RPC を切ると cormoran__pmw3610 が RPC サブシステム登録に載らないため、
# 13項目すべてが Studio に届かない（ファーム側の登録自体は正常なので気づきにくい）。
#
# CPU 負荷を理由に一度は無効化したが、上記のとおり機能そのものが成立しないため
# 有効化する。負荷の懸念自体も過大評価だった: 診断・生レジスタ・フレーム
# キャプチャの各ハンドラは Studio から明示的に要求されたときにしか動かず、
# 常時のオーバーヘッドは無い。DYA Studio にフレームビューアの UI は無いので
# （それはモジュール付属の別 Web アプリ側）、通常運用で叩かれることもない。
# 依存する CONFIG_ZMK_STUDIO=y は本ファイル下部の「ZMK Studio（必須ベース）」にある。
CONFIG_ZMK_PMW3610_STUDIO_RPC=y

# 「トラックボールをゆっくり動かすと反応が遅い」への対策。
# 既定の RUN_DOWNSHIFT_TIME_MS=128 では、無操作 128ms でサンプル周期 40ms の
# REST1 に落ちる。そっと動かし始めると 1 サンプル内に十分な信号が出ず、
# 動き出しが飲まれる（素早く弾くと即 RUN に戻るので違和感が出ない）。
# まず保守的に、RUN を維持する時間を延ばし REST1 の粗さを半分にする。
#
# ここでの値は「工場出荷時の既定値」でもあり、settings_reset 後もこの値に戻る。
# 実機での追い込みは DYA Studio 側で行い、良い値が固まったらここへ反映する。
# これで足りない場合は Studio 側で force_awake を試す（消費電力は増える）。
CONFIG_PMW3610_RUN_DOWNSHIFT_TIME_MS=2000
CONFIG_PMW3610_REST1_SAMPLE_TIME_MS=20

#Battery config -------------------
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_ZMK_BATTERY_REPORTING_FETCH_MODE_STATE_OF_CHARGE=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y

CONFIG_ZMK_IDLE_TIMEOUT=30000
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=7200000

##for NiMH battery
CONFIG_ZMK_NON_LIPO_BATTERY_MANAGEMENT=y

# optional
CONFIG_ZMK_NON_LIPO_MAX_MV=1360
CONFIG_ZMK_NON_LIPO_MIN_MV=1020
CONFIG_ZMK_NON_LIPO_LOW_MV=1000 
#CONFIG_ZMK_NON_LIPO_ADV_SLEEP_TIMEOUT=60000
# 60 seconds


#Layer LED config -------------------
CONFIG_RGBLED_WIDGET_BATTERY_LEVEL_HIGH=30
CONFIG_RGBLED_WIDGET_BATTERY_LEVEL_CRITICAL=10
CONFIG_RGBLED_WIDGET_SHOW_LAYER_COLORS=n


#Bluetooth config -------------------
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=12

# BLE コントローラの Prepare 遅延アサートを無効にする（Zephyr 既定は y）……はずだったが、
# 【未解決】この設定は実機には反映されない。ビルドログに以下の Kconfig 警告が出る:
#   warning: BT_CTLR_ASSERT_OVERHEAD_START ... was assigned the value 'n' but got the value 'y'.
# cormoran/zmk-feature-watchdog の DESIGN.md §12.1 に同一事象の調査記録があり、
# select/imply の類はツリー内に見つからず原因不明のまま、プロジェクトオーナーの
# 判断でオーバーライドの試みを断念している。つまり CONFIG_BT_CTLR_ASSERT_OVERHEAD_START
# は本リポジトリ・上流いずれの設定でも y のままで、無効化できていない。
#
# 実体: 4.1 系はモジュールが増えて CPU 負荷が重く、無線イベントの準備が間に合わないと
# lll_central.c / lll_adv.c の LL_ASSERT_OVERHEAD が k_oops() に落として再起動する
# （DESIGN.md によれば split central 構成 = BT_CENTRAL+PERIPHERAL+OBSERVER 同時稼働に
# 起因する Zephyr 本体側のマルチロール負荷下の脆弱性で、この設定リポジトリや
# zmk-feature-watchdog 自体のコードが原因ではない）。
# 実機のウォッチドッグに PC 0x2b232 (lll_central.c:250) と 0x2a514 (lll_adv.c:1041)
# で記録が残っていた。DYA Studio がいきなり切断される症状もこの再起動が原因と見られる。
#
# 根本修正手段が無いため、当面は CPU 負荷を下げて発生確率を下げる方向で緩和する
# （未使用の PHYSICAL_LAYOUTS_FEATURE を無効化、下記 WATCHDOG_FREEZE_MONITOR_LOWPRIO_QUEUE
# を無効化、など）。この行自体は将来 Kconfig 依存関係が解明された場合に備えて残す。
CONFIG_BT_CTLR_ASSERT_OVERHEAD_START=n

# ウォッチドッグのフリーズ検出タイマーを減らして上記クラッシュの発生確率を下げる。
# zmk-feature-watchdog の Kconfig ヘルプ（ZMK_WATCHDOG_FREEZE_FEED_DIVISOR）に、
# 「このモジュール自身の周期タイマーが、BLE コントローラの極めてタイトな無線イベント
# 準備タイミング（~275us）とたまたま衝突し、LL_ASSERT_OVERHEAD を誘発しうる」との
# 記載がある。低優先度キューの監視を止めてタイマー数を半減させる。システムワーク
# キューの監視（より重要なケース）はデフォルト通り継続する。
CONFIG_ZMK_WATCHDOG_FREEZE_MONITOR_LOWPRIO_QUEUE=n

# ========================================
# ZMK Studio（必須ベース）
# ========================================
CONFIG_ZMK_STUDIO=y
# CONFIG_ZMK_STUDIO_LOCKING=n
# 注意: CONFIG_ZMK_STUDIO_LOCKING=n を設定しないでください。
# Studio Locking は BLE 接続に必要なため、デフォルトの y を維持する必要があります。
# 詳細は「BLE 接続」セクションを参照。

# BLE 接続スロット（プロファイル + Peripheral + Studio）
CONFIG_BT_MAX_CONN=6
CONFIG_BT_MAX_PAIRED=6


# ========================================
# dya studioの各種機能
# ========================================
# Enable half-duplex UART mode provided
CONFIG_ZMK_SPLIT_WIRED_UART_MODE_INTERRUPT=y
# Enable split relay to unlock interacting with peripheral
CONFIG_ZMK_SPLIT_RELAY_EVENT=y

# For zephyr4.1 CONSOLE is enabled by default but not required
CONFIG_CONSOLE=n

#
# ZMK modules ready for custom RPC protocol
#

# https://github.com/cormoran/zmk-module-runtime-input-processor
CONFIG_ZMK_RUNTIME_INPUT_PROCESSOR=y
CONFIG_ZMK_RUNTIME_INPUT_PROCESSOR_STUDIO_RPC=y
# https://github.com/cormoran/zmk-module-ble-management
CONFIG_ZMK_BLE_MANAGEMENT=y
CONFIG_ZMK_BLE_MANAGEMENT_STUDIO_RPC=y
# https://github.com/cormoran/zmk-module-settings-rpc
CONFIG_ZMK_SETTINGS_RPC=y
CONFIG_ZMK_SETTINGS_RPC_STUDIO=y
# https://github.com/cormoran/zmk-feature-input-stream
CONFIG_ZMK_INPUT_STREAM_FEATURE=y
CONFIG_ZMK_INPUT_STREAM_FEATURE_STUDIO_RPC=y
# https://github.com/cormoran/zmk-feature-module-physical-layout
# CPU負荷軽減のため無効化（未使用機能）。BLEコントローラのLL_ASSERT_OVERHEAD
# クラッシュ対策の一環。詳細は本ファイル下部の CONFIG_BT_CTLR_ASSERT_OVERHEAD_START
# のコメントを参照。
# CONFIG_ZMK_PHYSICAL_LAYOUTS_FEATURE=y
# CONFIG_ZMK_PHYSICAL_LAYOUTS_FEATURE_STUDIO_RPC=y
# https://github.com/cormoran/zmk-feature-custom-settings
CONFIG_ZMK_CUSTOM_SETTINGS=y
CONFIG_ZMK_CUSTOM_SETTINGS_SPLIT_RPC_RELAY=y
CONFIG_ZMK_CUSTOM_SETTINGS_STUDIO_RPC=y
# https://github.com/cormoran/zmk-feature-runtime-combo
CONFIG_ZMK_RUNTIME_COMBO=y
CONFIG_ZMK_RUNTIME_COMBO_STUDIO_RPC=y
# https://github.com/cormoran/zmk-feature-runtime-macro
CONFIG_ZMK_RUNTIME_MACRO=y
CONFIG_ZMK_RUNTIME_MACRO_STUDIO_RPC=y
CONFIG_ZMK_BEHAVIOR_LOCAL_ID_TYPE_CRC16=y
# https://github.com/cormoran/zmk-feature-fast-keymap
CONFIG_ZMK_FAST_KEYMAP=y
CONFIG_ZMK_FAST_KEYMAP_STUDIO_RPC=y
CONFIG_ZMK_FAST_KEYMAP_DEFAULT_LAYER=y
# https://github.com/cormoran/zmk-feature-watchdog
CONFIG_ZMK_WATCHDOG=y
CONFIG_ZMK_WATCHDOG_STUDIO_RPC=y
# https://github.com/cormoran/zmk-feature-device-info
CONFIG_ZMK_DEVICE_INFO=y
CONFIG_ZMK_DEVICE_INFO_STUDIO_RPC=y
# https://github.com/cormoran/zmk-feature-os-detection
# central 側でのみ動作するため R.conf にのみ置く（L.conf には入れない）
CONFIG_ZMK_OS_DETECTION=y
CONFIG_ZMK_OS_DETECTION_USB=y
CONFIG_ZMK_OS_DETECTION_BLE=y
# iPad / iPhone を macOS と区別するための opt-in（BT_GATT_CLIENT を select する）
CONFIG_ZMK_OS_DETECTION_BLE_GATT_CLIENT_PROBE=y
CONFIG_ZMK_OS_DETECTION_STUDIO_RPC=y
# OS 別レイヤー自動切替: Windows 検出時だけ 1_base_win(index 1) をオーバーレイとして有効化する。
# macOS/iOS 検出時は 0_base_mac(index 0 = 常時有効な土台レイヤー) がそのまま使われるため、
# LAYER_MACOS / LAYER_IOS は既定値(-1・無効)のままでよい。
CONFIG_ZMK_OS_DETECTION_LAYER_AUTO_SWITCH=y
CONFIG_ZMK_OS_DETECTION_LAYER_WINDOWS=1
# https://github.com/cormoran/zmk-module-battery-history
# TODO: Re-enable after resolving slow save issue
# CONFIG_ZMK_BATTERY_HISTORY=y
# CONFIG_ZMK_BATTERY_HISTORY_STUDIO_RPC=y

#
# Adjustment for Custom RPC Protocol & Split Relay support
#

# Shorten time until persisting setting to prevent data loss after edit setting.
# WARNING: The downside is that it may cause more frequent writes to flash which may reduce flash lifespan.
CONFIG_ZMK_SETTINGS_SAVE_DEBOUNCE=10000
# Increase stack size to prevent stack overflow for split relay feature
CONFIG_ZMK_SPLIT_BLE_CENTRAL_SPLIT_RUN_STACK_SIZE=768
# Increase stack size for low priority thread to prevent stack overflow when encoding large notification body for custom rpc.
CONFIG_ZMK_LOW_PRIORITY_THREAD_STACK_SIZE=4096
# Increase RPC buffers for zmk-feature-custom-settings and runtime macro payloads.
CONFIG_ZMK_STUDIO_RPC_RX_BUF_SIZE=256
CONFIG_ZMK_STUDIO_RPC_TX_BUF_SIZE=256
CONFIG_ZMK_STUDIO_RPC_CUSTOM_SUBSYSTEM_REQUEST_PAYLOAD_MAX_BYTES=256
CONFIG_ZMK_SPLIT_RELAY_EVENT_DATA_LEN=240

CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=4096
CONFIG_ZMK_STUDIO_RPC_THREAD_STACK_SIZE=6000

CONFIG_ZMK_CUSTOM_SETTINGS_LARGE_VALUE_MAX_SIZE=256